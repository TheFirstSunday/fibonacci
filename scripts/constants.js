const ROLLUP_CONFIG_FILE = 'rollup.config.js';

const BABEL_CONFIG_FILE = 'babel.config.js';

const TS_CONFIG_FILE = 'tsconfig.json';

const jsExtensions = ['.ts', '.tsx', '.js', '.jsx'];

const ROLLUP_TYPE_WATCH = 'watch';

module.exports = {
  ROLLUP_CONFIG_FILE,
  BABEL_CONFIG_FILE,
  TS_CONFIG_FILE,
  jsExtensions,
  ROLLUP_TYPE_WATCH,
};
